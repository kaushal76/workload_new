{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h1>Module categories</h1>
                </div>
                <div class="panel-body"></div>
            </div>
        </div>
        <div class="col-md-8">
            <div class="panel panel-default">
                <div class="panel-heading"><h1>Module categories</h1></div>
                <div id="tablepanel" class="panel-body">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th width="5%">ID</th>
                            <th>Name</th>
                            <th width="20%">Code</th>
                            <th width="20%">Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for moduleCategory in moduleCategories %}
                            <tr>
                                <td>
                                    <a href="{{ path('modulecategory_show', { 'id': moduleCategory.id }) }}">{{ moduleCategory.id }}</a>
                                </td>
                                <td>{{ moduleCategory.name }}</td>
                                <td>{{ moduleCategory.code }}</td>
                                <td>
                                    <a class="btn btn-primary"
                                       href="{{ path('modulecategory_show', { 'id': moduleCategory.id }) }}">Show</a>
                                    <a class="btn btn-primary"
                                       href="{{ path('modulecategory_edit', { 'id': moduleCategory.id }) }}">Edit</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div>
                <a class="btn btn-primary" href="{{ path('modulecategory_new') }}">Create a new</a>
            </div>
        </div>

    </div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('javascript/allocations.js') }}"></script>
{% endblock %}
