{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modulecategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ moduleCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ moduleCategory.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ moduleCategory.code }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('modulecategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('modulecategory_edit', { 'id': moduleCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
