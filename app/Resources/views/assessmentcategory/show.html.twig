{% extends 'base.html.twig' %}

{% block body %}
    <h1>Assessmentcategory</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ assessmentCategory.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ assessmentCategory.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ assessmentCategory.code }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('assessmentcategory_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('assessmentcategory_edit', { 'id': assessmentCategory.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
