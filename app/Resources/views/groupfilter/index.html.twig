{% extends '::base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-4">
            <div class="panel panel-default">
                <div class="panel-heading">
                    <h1>Group filters</h1>
                </div>
                <div class="panel-body"></div>
            </div>
        </div>
        <div class="col-md-8">
            <div class="panel panel-default">
                <div class="panel-heading"><h1>Group filters</h1></div>
                <div id="tablepanel" class="panel-body">
                    <table class="table table-striped">
                        <thead>
                        <tr>
                            <th width="5%">ID</th>
                            <th>Name</th>
                            <th width="20%">Code</th>
                            <th width="20%">Actions</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for groupFilter in groupFilters %}
                            <tr>
                                <td>
                                    <a href="{{ path('groupfilter_show', { 'id': groupFilter.id }) }}">{{ groupFilter.id }}</a>
                                </td>
                                <td>{{ groupFilter.name }}</td>
                                <td>{{ groupFilter.code }}</td>
                                <td>
                                    <a class="btn btn-primary"
                                       href="{{ path('groupfilter_show', { 'id': groupFilter.id }) }}">Show</a>
                                    <a class="btn btn-primary"
                                       href="{{ path('groupfilter_edit', { 'id': groupFilter.id }) }}">Edit</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            <div>
                <a class="btn btn-primary" href="{{ path('groupfilter_new') }}">Create a new</a>
            </div>
        </div>

    </div>
{% endblock %}
{% block javascripts %}
    <script src="{{ asset('javascript/allocations.js') }}"></script>
{% endblock %}
